# RdpGuard v0.3.1 验收摘要

验收日期：2026-08-05

## 默认策略

- 每 60 秒检查一次。
- 同一公网 IP 在最近 10 分钟内失败 5 次时封禁 360 分钟。
- 到期自动解封，只管理 `RdpGuard AutoBlock <IP>` 防火墙规则。

## 新增监控功能

- `rdpguard-monitor.exe` 是按需运行的只读终端工具，不进入后台服务进程。
- IP 概览分别显示登录尝试、成功、失败、防护失败、当前连接和封禁状态。
- 登录事件读取 Security 4624/4625，并限制为 RDP LogonType 10。
- 防护失败读取 RdpCoreTS 140，与服务的实际封禁统计一致。
- 当前连接通过 Windows IP Helper API 读取已建立的 IPv4/IPv6 RDP TCP 会话。
- 默认查看最近 1 小时，每 30 秒刷新，可切换 10 分钟、24 小时和 7 天。
- 每个事件源最多处理 50,000 条记录，错误和截断在界面单独提示。

## 自动化覆盖

- 共 52 项 Rust 测试，覆盖事件解析、IP 聚合、原生连接转换、三个终端页面、部分数据源失败、刷新、服务策略、回滚、安装和发布包内容。
- 验证同一 IP 五次登录且全部失败时显示 `尝试=5、成功=0、失败=5`。
- 验证 Security 登录与事件 140 分开统计，不重复相加。
- 验证较晚动作失败时，较早已成功动作仍立即写入日志。
- 验证监控器不调用封禁、解封或状态写入接口。
- 验证非管理员安装器和监控器的 UAC 启动路径。

## 发布程序

正式 GitHub Release 由 Windows Actions 使用锁定依赖重新构建，两个程序的 `--version` 输出均须与 Git 标签一致。

不同 Windows 构建环境生成的 PE 文件不承诺具有相同的逐文件哈希。官方下载校验以对应 Release 附带的 `SHA256SUMS.txt` 为准；该文件记录整个 ZIP 的 SHA-256。
