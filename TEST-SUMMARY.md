# RdpGuard v0.3.2 验收摘要

验收日期：2026-08-05

## 默认策略

- 每 60 秒检查一次。
- 同一公网 IP 在最近 10 分钟内失败 5 次时封禁 360 分钟。
- 到期自动解封，只管理 `RdpGuard AutoBlock <IP>` 防火墙规则。

## 在线入口与配置

- `irm` 入口在下载前显示安装、历史日志、语言和退出菜单。
- ZIP 的 SHA-256 必须匹配同版本 `SHA256SUMS.txt`，不匹配时不会执行。
- 配置向导覆盖检查间隔、统计窗口、失败阈值、封禁时长和白名单。
- 已安装配置可读取和保留；非法范围/IP 被拒绝；服务安装失败时恢复旧配置。
- 中文/英文根据 Windows 自动选择，也可在主菜单和监控器中手动切换。

## 历史日志监控器

- `rdpguard-monitor.exe` 是按需运行的只读工具，不进入后台服务进程。
- 只显示 IP 历史概览与登录事件，不采集实时 TCP 连接。
- Security 4624/4625 与 RdpCoreTS 140 分开统计，避免重复累计。
- 启动时读取一次；按 `r` 或切换时间范围才重新读取，不定时刷新。
- 每个事件源最多处理 50,000 条，部分数据源失败时仍显示可用结果。

## 自动化覆盖

- Rust 测试覆盖事件解析、IP 聚合、双语双页面、手动刷新、服务策略、回滚、安装和发布清单。
- PowerShell 测试覆盖配置范围、白名单、语言、菜单路由、哈希匹配、缓存复用和临时目录清理。
- 发布前运行格式检查、Clippy、全部测试、release 构建、PowerShell 语法和工作流检查。

## 发布校验

GitHub Release 由 Windows Actions 使用锁定依赖构建。官方下载校验以对应 Release 附带的 `SHA256SUMS.txt` 为准；两个 EXE 的 `--version` 必须与 Git 标签一致。
