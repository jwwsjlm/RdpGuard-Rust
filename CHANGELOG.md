# Changelog

## 0.4.4 - 2026-08-05

- 在线菜单的历史监控器改为在独立、可见的管理员控制台中运行，避免与临时提权 PowerShell 共享控制台时收到 `0xC000013A` 并立即退出。
- 在线菜单在打开监控器前显示进度，并在配置完成、监控窗口关闭或诊断完成后给出明确的中英文反馈。
- 修复中文菜单“运行诊到”的错字。

## 0.4.3 - 2026-08-05

- 修复规则到期元数据使用 `|` 分隔符时被 Windows 防火墙以 `0x80070057` 拒绝、导致旧封禁规则删除后无法重建的问题；新格式改用兼容分隔符，同时仍可读取旧格式。
- 服务初始化失败时向 SCM 明确报告 `Stopped` 和非零退出码，不再永久停留在 `START_PENDING`。
- 安装器使用 `sc.exe` 与 30 秒有界轮询启动服务，启动失败会显示退出码和日志位置并进入事务回滚；首次健康启动前暂时关闭非崩溃恢复动作，避免安装期间重启循环。
- 停止服务也改为有界轮询，超时后仅终止对应服务 PID，防止升级或回滚无限等待。

## 0.4.2 - 2026-08-05

- 修复事务升级预检副本未以 `.exe` 结尾，导致 PowerShell 未执行新服务程序并触发 `UPGRADE001` 回滚的问题。
- 预检失败现在会报告文件路径、退出码、启动异常、实际输出和期望版本，便于识别安全软件拦截、文件损坏或架构错误。
- 升级与回滚错误只保留一个稳定错误码前缀，不再显示重复的 `UPGRADE001: UPGRADE001:`。

## 0.4.1 - 2026-08-05

- 修复安装向导把仅完成 TCP 握手的攻击地址误认为当前 RDP 来源的问题；白名单建议现在只读取 WTS 中已认证且当前活动的 RDP 会话，并明确要求人工确认客户端报告的地址。
- 修复旧防火墙规则缺少到期元数据时，监控器显示 `1970-01-01 08:00:00` 的问题；优先显示状态文件中的真实解封时间，否则显示 `-`。
- `doctor` 的当前来源检查同步改用已认证活动 RDP 会话，不再把普通 3389 TCP 连接描述为登录会话。

## 0.4.0 - 2026-08-05

- 防火墙规则与状态自动对账：重建缺失规则、替换错误范围/端口、清理到期/白名单/孤立规则；状态损坏时隔离并从规则元数据恢复。
- 新增 CIDR、IPv4-mapped IPv6 规范化、5000 条容量保护，以及 6 小时起步、2 倍递增、最长 7 天、30 天重置的复犯策略。
- 支持 `all_inbound` 和仅实际 RDP TCP/UDP 端口的 `rdp_only`，默认从注册表读取端口。
- 新增只读 `rdpguard doctor` 文本/JSON 诊断和在线菜单 `[4]`，错误统一使用稳定代码与修复建议。
- 服务首次健康检查完成后才报告 Running；日志改为动作/错误即时记录、相同错误抑制和每小时心跳。
- 在线流程先 UAC，再在受保护 ProgramData 暂存区下载校验；本地安装器不再从用户可写目录自提权。
- 升级保留服务对象并执行预检、备份、替换、真实就绪等待和完整回滚。
- 发布 x64、ARM64、x86 静态 CRT 包，增加 PowerShell 5.1、cargo audit/deny、CodeQL、Actionlint、SBOM、SHA-256 与 GitHub build provenance。
- 源码仓库移除旧的 0.3.1 EXE 构建产物。

## 0.3.7 - 2026-08-05

- 修复 UAC 提权后的交互式配置向导被错误地以 `-NonInteractive` 启动、导致 `Read-Host` 失败的问题。
- 提权安装失败时将子进程的详细错误回传到原窗口，不再只显示退出码。
- 服务日志支持按大小自动轮转；默认单文件 10 MB，并保留 5 个历史文件，可在配置向导中调整。

## 0.3.6 - 2026-08-05

- 将 IP 概览中的“防护失败”改名为更直观的“RDP 失败”。
- 快捷键提示改为自适应两行或三行布局，修复中文终端中滚动和退出提示被截断的问题。

## 0.3.5 - 2026-08-05

- 修复在线入口无法解析 Release 校验文件 `CRLF` 行尾的问题。
- 在线安装器测试同时覆盖 Windows `CRLF` 与 Unix `LF` 校验文件。

## 0.3.4 - 2026-08-05

- 在线入口改为纯 ASCII 源码并在运行时解码中文文案，同时兼容 `irm` 和 Windows PowerShell 5.1 文件执行。
- CI 与 Release 工作流新增在线入口 ASCII 兼容性检查。

## 0.3.3 - 2026-08-05

- 修复在线入口带 UTF-8 BOM 时无法通过 `irm` 创建脚本块的问题。
- PowerShell 测试和 Release 工作流新增无 BOM 与完整脚本解析检查，防止在线入口回归。

## 0.3.2 - 2026-08-05

- 新增一行 `irm` 在线入口，提供安装/配置服务、查看历史日志、切换语言和退出菜单。
- 在线入口直接下载同一正式版本的 ZIP 与 `SHA256SUMS.txt`，校验通过后才执行，并在退出时清理临时文件。
- 安装器新增中英文五项配置向导，支持读取当前配置、回车保留、白名单校验和安装失败回滚。
- 历史日志监控器支持自动选择及手动切换中英文，只保留 IP 历史概览和登录事件两个页面。
- 移除当前 TCP 连接采集和定时刷新；日志只在启动、切换时间范围或按 `r` 时读取。

## 0.3.1 - 2026-08-05

- 修正验收摘要中的校验说明：不同 Windows runner 生成的 PE 文件不承诺逐文件可复现哈希。
- 官方下载校验统一以每个 GitHub Release 附带的 `SHA256SUMS.txt` 为准。

## 0.3.0 - 2026-08-05

- 新增只读终端工具 `rdpguard-monitor.exe`，按 IP 展示登录成功、登录失败、防护失败、当前连接和封禁状态。
- 提供登录事件、IP 概览、当前 RDP 连接三个页面，支持时间范围切换、30 秒自动刷新和手动刷新。
- 登录统计读取 Security 4624/4625，封禁计数继续使用 RdpCoreTS 140，避免重复计数。
- 通过 Windows IP Helper API 读取当前 IPv4/IPv6 RDP TCP 连接，不启动 `netstat` 子进程。
- 封禁和解封成功后写入包含 IP、失败次数和到期时间的操作日志。
- 安装器和 Release 压缩包同时包含服务程序和监控程序，README 改为简明使用教程。

## 0.2.2 - 2026-08-04

- 安装器在非管理员 PowerShell 中运行时自动请求 UAC 提权。
- 使用编码命令安全传递包含空格、中文或单引号的安装器路径。
- 等待提权进程结束并检查退出码，取消 UAC 或安装失败时返回明确错误。

## 0.2.1 - 2026-08-04

- 移除 Dependabot 每周依赖检查。
- 新增标签触发的 GitHub Release 工作流，自动发布 Windows ZIP 和 SHA-256 校验文件。

## 0.2.0 - 2026-08-04

- 新增 `check_interval_seconds`，检查间隔可在 10–3600 秒内配置。
- 为统计窗口、失败阈值和封禁时长增加安全范围校验。
- 将 CLI 版本测试改为自动读取 Cargo 包版本。
- 补充完整中文安装、配置、升级、构建和 GitHub 发布教程。
- 增加 Git 仓库忽略规则、属性和 MIT 许可证。
- 将全部 Cargo 依赖更新并显式声明为 2026-08-04 的最新稳定版本。
- 新增 Windows GitHub Actions CI、release 构件上传和 Dependabot 自动更新。

## 0.1.0 - 2026-08-04

- 首个 Rust Windows 服务版本。
- 支持 RDP 失败事件聚合、临时防火墙封禁、自动解封、白名单和状态持久化。
- 提供 PowerShell 安装器、卸载器和只读 dry-run。
